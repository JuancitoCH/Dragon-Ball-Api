// import dotenv from 'dotenv'
// dotenv.config({
//     path:require('path')
// 		.join(__dirname,'..','..','.env')
// })

export default {
	PORT:process.env.PORT,
	BASIC_KEY_USERNAME:process.env.BASIC_KEY_USERNAME,
	BASIC_KEY_PASSWORD:process.env.BASIC_KEY_PASSWORD,
}